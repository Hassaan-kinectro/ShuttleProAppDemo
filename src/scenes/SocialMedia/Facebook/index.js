import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Facebook = () => {
  return (
    <View
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        color: 'red',
      }}>
      <Text style={{color: '#fff'}}>FbTab</Text>
    </View>
  );
};

export default Facebook;
